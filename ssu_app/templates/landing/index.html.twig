{% extends 'base.html.twig' %}

{% block title %}Hello LandingController!
{% endblock %}
{# {{ dump(content) }} #}
{% block body %}
	<div class="container col-xxl-8 px-4 py-5">
		<div class="row flex-lg-row-reverse align-items-center g-5 py-5">
			<div class="col-10 col-sm-8 col-lg-6">
				<img src="{{ asset('assets/img/screenShot-file-landing.png')}}" class="d-block mx-lg-auto img-fluid rounded-3" alt="Screenshot images" width="700" height="500" loading="lazy">
			</div>
			<div class="col-lg-6">
				<h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">{{ h1 }}</h1>
				<p class="lead">{{ content }}</p>
				<div class="d-grid gap-2 d-md-flex justify-content-md-start">
					<a href="{{ cta }}" type="button" class="btn btn-primary btn-lg px-4 me-md-2">Upload form</a>
					<a href="{{ list_of_uploaded_image }}" type="button" class="btn btn-light btn-lg px-4 me-md-2">list of images</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
